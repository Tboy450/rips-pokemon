[project]
name = "triumph-rips-ai"
version = "0.1.0"
description = "Strategy simulator and manual tracker for Rips by Triumph pack cycling."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
rips-ai = "rips_ai.cli:main"

